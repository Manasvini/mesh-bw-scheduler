apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-flask
spec:
  replicas: 1
  selector:
    matchLabels:
      app: simple-flask
  template:
    metadata:
      labels:
        app: simple-flask
    spec:
      containers:
      - name: simple-flask
        image: 192.168.160.23:5000/simple_flask_container
        ports:
        - containerPort: 6000
---
apiVersion: v1
kind: Service
metadata:
  name: simple-flask-entrypoint
  namespace: default
spec:
  type: NodePort
  selector:
    bb: web
  ports:
  - port: 6000
    targetPort: 6000
    nodePort: 30000
